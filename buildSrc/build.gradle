plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        myPlugins {
            id = 'compiler.native-image'
            implementationClass = 'compiler.NativeImagePlugin'
        }
    }
}
